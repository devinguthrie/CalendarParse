<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true">

		<!-- NotificationListenerService — user must grant "Notification access" in system settings -->
		<service
			android:name="calendarparse.platforms.android.AndroidNotificationMonitor"
			android:label="CalendarParse Notification Monitor"
			android:exported="true"
			android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
			<intent-filter>
				<action android:name="android.service.notification.NotificationListenerService" />
			</intent-filter>
		</service>

		<!-- Job polling foreground service — runs while schedule is being processed on the server -->
		<service
			android:name="calendarparse.platforms.android.JobPollingForegroundService"
			android:foregroundServiceType="dataSync"
			android:exported="false" />

		<!-- Handles Yes / No action buttons on the "Schedule image received" notification -->
		<receiver
			android:name="calendarparse.platforms.android.ScheduleImageActionReceiver"
			android:exported="false">
			<intent-filter>
				<action android:name="calendarparse.action.MONITOR_YES" />
				<action android:name="calendarparse.action.MONITOR_NO" />
			</intent-filter>
		</receiver>

</application>

	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
	<uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
	<uses-permission android:name="android.permission.READ_CALENDAR" />
	<uses-permission android:name="android.permission.WRITE_CALENDAR" />
	<uses-permission android:name="android.permission.READ_SMS" />
	<uses-permission android:name="android.permission.READ_CONTACTS" />
	<uses-feature android:name="android.hardware.camera" android:required="false" />

	<!--
	    Android 11+ Package Visibility (API 30+): without <queries> the PackageManager
	    returns nothing for QueryIntentActivities and throws NameNotFoundException for
	    GetPackageInfo on every package not in this list.
	-->
	<queries>
		<!-- Discover native SMS apps via intent -->
		<intent>
			<action android:name="android.intent.action.SENDTO" />
			<data android:scheme="smsto" />
		</intent>

		<!-- Well-known third-party messaging apps -->
		<package android:name="com.whatsapp" />
		<package android:name="com.whatsapp.w4b" />
		<package android:name="org.telegram.messenger" />
		<package android:name="org.thoughtcrime.securesms" />
		<package android:name="com.facebook.orca" />
		<package android:name="com.facebook.mlite" />
		<package android:name="com.viber.voip" />
		<package android:name="com.discord" />
		<package android:name="com.instagram.android" />
		<package android:name="jp.naver.line.android" />
		<package android:name="com.kakao.talk" />
		<package android:name="com.snapchat.android" />
	</queries>
</manifest>
